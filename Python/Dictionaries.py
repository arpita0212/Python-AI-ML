thisDict = {
    "brand": "Ford",
    "model": "Mustang",
    "year": 1964
}
print(thisDict)

# Dictionaries items
print(thisDict["brand"])

# Dictionaries 
print(len(thisDict))

# dictionaries type
print(type(thisDict))

# Accessing items
X = thisDict["model"]
print(X);

# Get the value of the "model" key:

X = thisDict.get("model");